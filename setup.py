from setuptools import setup, find_packages

setup(
    name='integration-tester',
    version='0.0.1',
    description='A package that helps you test services integratively',
    packages=find_packages(),
)
